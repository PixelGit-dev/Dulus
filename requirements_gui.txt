# Dulus GUI dependencies
# Install with: pip install -r requirements_gui.txt

# Modern web GUI (dulus --gui) — pywebview shell around the React build
pywebview>=5.0

# Classic desktop GUI (dulus --gui-classic)
customtkinter>=5.2.2
Pillow>=10.0.0
